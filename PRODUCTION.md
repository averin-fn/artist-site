# 🚀 Подготовка к Production

## ⚠️ Важные шаги перед деплоем:

### 1. Безопасность

#### Измените JWT_SECRET
```bash
cd server
```

Отредактируйте `server/.env`:
```env
JWT_SECRET=сгенерируйте_длинный_случайный_ключ_минимум_32_символа
```

Генерация безопасного ключа:
```powershell
# PowerShell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | % {[char]$_})
```

#### Измените пароль администратора
После первого запуска сразу смените пароль в админке или через базу данных:
```javascript
const bcrypt = require('bcryptjs');
const newPasswordHash = bcrypt.hashSync('ваш_новый_безопасный_пароль', 10);
// Обновите в базе данных
```

### 2. Конфигурация API URL

#### Frontend (src/services/api.js)
Измените хардкод URL на ваш production домен:
```javascript
const API_URL = 'https://your-domain.com/api';
```

### 3. База данных

#### Бэкапы
Настройте автоматическое резервное копирование:
```bash
# Создайте cron job или задачу планировщика
cp server/artist.db server/backups/artist_$(date +%Y%m%d_%H%M%S).db
```

#### Миграции
При изменении структуры БД создавайте миграционные скрипты в `server/migrations/`

### 4. CORS настройки

Отредактируйте `server/server.js` для ограничения CORS:
```javascript
app.use(cors({
  origin: ['https://your-domain.com', 'https://www.your-domain.com'],
  credentials: true
}));
```

### 5. Переменные окружения

Создайте отдельный `.env` для production:
```env
PORT=3001
JWT_SECRET=ваш_длинный_случайный_секретный_ключ
ADMIN_USERNAME=admin
ADMIN_PASSWORD=безопасный_пароль
UPLOAD_PATH=../public/uploads
NODE_ENV=production
```

### 6. Ограничения загрузки файлов

В `server/routes/upload.js` уже настроено:
- Максимальный размер: 10MB
- Типы файлов: jpg, jpeg, png, gif, webp

Для production рассмотрите:
- Использование CDN для изображений
- Оптимизацию изображений (sharp, imagemin)
- Проверку на вирусы

### 7. Логирование

Установите и настройте Winston или Morgan:
```bash
cd server
npm install winston morgan
```

### 8. Process Manager

Используйте PM2 для управления процессом:
```bash
npm install -g pm2
pm2 start server/server.js --name artist-site
pm2 startup
pm2 save
```

### 9. HTTPS

Обязательно используйте HTTPS в production:
- Let's Encrypt (бесплатно)
- Cloudflare
- Nginx/Apache с SSL сертификатом

### 10. Мониторинг

Настройте мониторинг:
- Uptime проверки (uptimerobot.com)
- Логи ошибок (Sentry)
- Метрики производительности

## 📋 Чеклист перед деплоем:

- [ ] JWT_SECRET изменен на случайный длинный ключ
- [ ] Пароль админа изменен
- [ ] API_URL обновлен на production домен
- [ ] CORS настроен для конкретных доменов
- [ ] Настроены бэкапы базы данных
- [ ] Установлен PM2 или аналог
- [ ] Настроен HTTPS
- [ ] Настроено логирование
- [ ] Проверена безопасность файловых загрузок
- [ ] Настроен мониторинг

## 🔧 Возможные улучшения:

1. **Rate Limiting** - ограничение частоты запросов
2. **Helmet.js** - дополнительная безопасность HTTP заголовков
3. **Compression** - сжатие ответов
4. **Redis** - кеширование для улучшения производительности
5. **S3/Cloud Storage** - хранение изображений в облаке
6. **Image optimization** - автоматическая оптимизация при загрузке
7. **Email notifications** - уведомления о новых заказах/сообщениях
8. **Analytics** - Google Analytics или аналоги
9. **SEO optimization** - meta tags, sitemap, robots.txt
10. **Progressive Web App** - offline support

## 📊 Рекомендуемый стек для хостинга:

- **Backend**: Railway, Render, Heroku, DigitalOcean
- **Frontend**: Netlify, Vercel, Cloudflare Pages
- **Database**: Файловая SQLite (для малых проектов) или PostgreSQL/MySQL
- **Storage**: AWS S3, Cloudinary, DigitalOcean Spaces
