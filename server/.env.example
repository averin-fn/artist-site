# Backend Configuration

# Server Port
PORT=3001

# JWT Secret Key (ВАЖНО: измените на длинный случайный ключ в production!)
# Генерация: openssl rand -base64 64
JWT_SECRET=your_secret_key_change_this_in_production_use_long_random_string_64_plus_characters

# Admin Credentials (ВАЖНО: измените после первого входа!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Upload Path (относительно server/)
UPLOAD_PATH=../public/uploads

# Environment
NODE_ENV=development
