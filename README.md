# 🎨 Artist Site - Сайт художника с админ-панелью

Полнофункциональный сайт художника с React фронтендом, Node.js бекендом, SQLite базой данных и админ-панелью для управления контентом.

## 🚀 Быстрый старт

### 1. Backend
```bash
cd server
npm install
npm run init-db    # Создаст БД и админа (admin/admin123)
npm start          # Запустит на http://localhost:3001
```

### 2. Frontend
```bash
npm install
npm start          # Запустит на http://localhost:3000
```

## 📍 Адреса

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3001/api
- **Админ-панель**: http://localhost:3001/admin
  - Логин: `admin`
  - Пароль: `admin123`

## ✨ Возможности

- ✅ Управление работами художника (создание, редактирование, удаление)
- ✅ Загрузка и управление множественными изображениями
- ✅ Редактирование информации "О художнике"
- ✅ Управление галереей изображений в разделе "О художнике"
- ✅ JWT аутентификация
- ✅ Автоматическое обновление данных в админке
- ✅ Защита от XSS атак
- ✅ Транзакционная работа с базой данных
- ✅ Валидация загружаемых файлов (до 10MB)

## 🏗️ Технологии

### Frontend
- **React 18** + **TypeScript** - UI библиотека с типизацией
- **Webpack 5** - Сборщик модулей

### Backend
- **Node.js** + **Express** - Серверный фреймворк
- **SQLite3** (better-sqlite3) - База данных с WAL режимом
- **JWT** - Аутентификация
- **Multer** - Загрузка файлов
- **bcryptjs** - Хеширование паролей

## 📂 Структура проекта

```
├── src/                    # Frontend исходники
│   ├── pages/              # Страницы (Gallery, About)
│   ├── components/         # Компоненты (Header, Modal, PaintingCard)
│   ├── services/           # API сервисы
│   └── assets/             # Изображения
├── server/                 # Backend
│   ├── server.js           # Главный сервер
│   ├── database.js         # Конфигурация БД
│   ├── routes/             # API роуты
│   ├── middleware/         # Middleware (auth)
│   ├── admin/              # Админ-панель (HTML/CSS/JS)
│   └── scripts/            # Скрипты инициализации
└── public/                 # Статические файлы

```

## 🔒 Безопасность

- JWT токены с expiration
- Bcrypt хеширование паролей
- XSS защита с экранированием HTML
- Валидация размера файлов (макс. 10MB)
- CORS настроен для localhost

## 🛠️ API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему

### Работы
- `GET /api/artworks` - Получить все работы
- `POST /api/artworks` - Создать работу
- `PUT /api/artworks/:id` - Обновить работу
- `DELETE /api/artworks/:id` - Удалить работу

### О художнике
- `GET /api/about` - Получить информацию
- `PUT /api/about` - Обновить информацию

### Загрузка
- `POST /api/upload` - Загрузить изображение

## � Деплой на VPS

Проект настроен для автоматического деплоя через GitHub Actions:

### Быстрый старт
1. Настройте VPS (Node.js, PM2, Git)
2. Добавьте секреты в GitHub (VPS_HOST, VPS_USERNAME, VPS_SSH_KEY, JWT_SECRET)
3. Push в ветку `main` → автоматический деплой!

### Документация
- [📘 Полная инструкция по деплою](.github/workflows/README.md)
- [💻 Примеры команд](.github/DEPLOY_COMMANDS.md)
- [🏭 Production чеклист](PRODUCTION.md)

### Workflows
- `.github/workflows/deploy.yml` - Основной деплой через Git Pull
- `.github/workflows/deploy-rsync.yml` - Альтернативный деплой через Rsync

## 📝 Примечания

- По умолчанию создается пользователь: `admin` / `admin123`
- Измените пароль после первого входа!
- База данных: `server/artist.db`
- Загруженные изображения: `public/uploads/`
- **Webpack Dev Server** - Development server with HMR
- **ts-loader** - TypeScript loader for webpack
